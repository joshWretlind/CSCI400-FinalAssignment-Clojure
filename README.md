CSCI400-FinalAssignment-Clojure
===============================

A repo for the final assignment for CSCI400, which we did in Clojure

Clojure Scripts
---------------

To run clojure scripts:

`$ java -cp clojure.jar clojure.main <script>.clj`
